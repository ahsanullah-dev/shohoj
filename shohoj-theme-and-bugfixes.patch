diff --git a/frontend/css/style.css b/frontend/css/style.css
index 9f676cf..4be4e53 100644
--- a/frontend/css/style.css
+++ b/frontend/css/style.css
@@ -188,15 +188,15 @@ button, input, select, textarea {
 .blob {
   position: absolute;
   border-radius: 50%;
-  filter: blur(65px);
-  opacity: 0.5;
+  filter: blur(90px);
+  opacity: 0.26;
   will-change: transform;
 }
 .blob.b1 { width: 440px; height: 440px; top: -100px; left: -100px; background: radial-gradient(circle, var(--amber), transparent 70%); animation: drift1 24s ease-in-out infinite; }
 .blob.b2 { width: 400px; height: 400px; top: 18%; right: -120px; background: radial-gradient(circle, var(--teal), transparent 70%); animation: drift2 28s ease-in-out infinite; }
-.blob.b3 { width: 360px; height: 360px; top: 52%; left: 3%; background: radial-gradient(circle, var(--violet), transparent 70%); animation: drift3 32s ease-in-out infinite; }
-.blob.b4 { width: 320px; height: 320px; top: 78%; right: 8%; background: radial-gradient(circle, var(--rose), transparent 70%); animation: drift1 26s ease-in-out infinite reverse; }
-.blob.b5 { width: 280px; height: 280px; top: 35%; left: 45%; background: radial-gradient(circle, var(--cyan), transparent 70%); animation: drift2 30s ease-in-out infinite reverse; }
+.blob.b3 { width: 360px; height: 360px; top: 52%; left: 3%; background: radial-gradient(circle, var(--violet), transparent 70%); animation: drift3 32s ease-in-out infinite; opacity: 0.7; }
+.blob.b4 { width: 320px; height: 320px; top: 78%; right: 8%; background: radial-gradient(circle, var(--rose), transparent 70%); animation: drift1 26s ease-in-out infinite reverse; opacity: 0.6; }
+.blob.b5 { width: 280px; height: 280px; top: 35%; left: 45%; background: radial-gradient(circle, var(--cyan), transparent 70%); animation: drift2 30s ease-in-out infinite reverse; opacity: 0.5; }
 
 @keyframes drift1 {
   0%, 100% { transform: translate(0, 0) rotate(0deg) scale(1); }
@@ -414,7 +414,7 @@ nav, #nav {
 .notif-bell-btn {
   background: var(--surface-2);
   border: 1px solid var(--line);
-  color: var(--text);
+  color: var(--muted);
   width: 38px;
   height: 38px;
   border-radius: 50%;
@@ -423,26 +423,39 @@ nav, #nav {
   justify-content: center;
   cursor: pointer;
   position: relative;
-  font-size: 16px;
-  transition: background 0.2s var(--ease), border-color 0.2s var(--ease);
+  transition: background 0.2s var(--ease), border-color 0.2s var(--ease), color 0.2s var(--ease), transform 0.2s var(--ease);
+}
+.notif-bell-btn svg {
+  width: 18px;
+  height: 18px;
 }
 .notif-bell-btn:hover {
   background: var(--surface-3);
-  border-color: var(--amber);
+  border-color: var(--line-bright);
+  color: var(--text);
+}
+.notif-bell-btn.has-unread {
+  color: var(--amber);
+  border-color: rgba(255, 176, 32, 0.35);
+}
+.notif-bell-btn:active {
+  transform: scale(0.94);
 }
 .notif-badge {
   position: absolute;
-  top: -4px;
-  right: -4px;
+  top: -3px;
+  right: -3px;
   background: var(--rose);
   color: #FFFFFF;
   font-family: 'JetBrains Mono', monospace;
   font-size: 10px;
   font-weight: 700;
-  padding: 1px 5px;
+  line-height: 1;
+  padding: 3px 5px;
   border-radius: 10px;
   min-width: 16px;
   text-align: center;
+  border: 2px solid var(--nav-bg);
   box-shadow: 0 2px 6px rgba(251, 113, 133, 0.4);
 }
 .notif-panel {
@@ -1015,10 +1028,10 @@ nav, #nav {
   border-radius: 100px;
   width: fit-content;
 }
-.badge.open    { background: var(--teal-dim); color: var(--teal); }
-.badge.matched { background: var(--amber-dim); color: var(--amber); }
-.badge.done    { background: rgba(139, 147, 167, 0.14); color: var(--muted); }
-.badge.ruet    { background: var(--violet-dim); color: var(--violet); }
+.badge.open, .badge-open       { background: var(--teal-dim); color: var(--teal); }
+.badge.matched, .badge-matched { background: var(--amber-dim); color: var(--amber); }
+.badge.done, .badge-done       { background: rgba(139, 147, 167, 0.14); color: var(--muted); }
+.badge.ruet, .badge-ruet       { background: var(--violet-dim); color: var(--violet); }
 .badge .d {
   width: 6px;
   height: 6px;
@@ -1173,6 +1186,7 @@ nav, #nav {
   overflow: hidden;
   display: flex;
   flex-direction: column;
+  cursor: pointer;
   transition: transform 0.3s var(--ease), box-shadow 0.3s var(--ease), border-color 0.3s var(--ease);
 }
 .post-card:hover {
@@ -1180,6 +1194,14 @@ nav, #nav {
   border-color: rgba(255, 176, 32, 0.35);
   box-shadow: var(--shadow);
 }
+.post-card:focus-visible {
+  outline: 2px solid var(--amber);
+  outline-offset: 2px;
+}
+.post-card .meta a, .post-card .thumb {
+  position: relative;
+  z-index: 2;
+}
 .post-card .thumb {
   height: 180px;
   background-color: var(--surface-2);
diff --git a/frontend/feed.html b/frontend/feed.html
index bba8e79..5aeb200 100644
--- a/frontend/feed.html
+++ b/frontend/feed.html
@@ -181,7 +181,7 @@
           ? fmtBDT(p.price) + (p.priceNote ? ' · ' + esc(p.priceNote) : '')
           : '';
         return `
-          <article class="post-card">
+          <article class="post-card" data-post-id="${p._id}" tabindex="0" role="link" aria-label="${esc(p.title)}">
             ${thumb}
             <div class="body">
               <h3><a href="post.html?id=${p._id}">${esc(p.title)}</a></h3>
@@ -200,6 +200,21 @@
           </article>
         `;
       }).join('');
+
+      wireCardNavigation(feed);
+    }
+
+    function wireCardNavigation(container) {
+      container.querySelectorAll('.post-card').forEach((card) => {
+        const go = () => { location.href = 'post.html?id=' + card.dataset.postId; };
+        card.addEventListener('click', go);
+        card.addEventListener('keydown', (e) => {
+          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); go(); }
+        });
+        card.querySelectorAll('a, .thumb').forEach((el) => {
+          el.addEventListener('click', (e) => e.stopPropagation());
+        });
+      });
     }
 
     ['q', 'category', 'postType', 'ruetOnly'].forEach((id) => {
@@ -220,6 +235,14 @@
     });
     document.getElementById('cancelBtn').addEventListener('click', () => modal.classList.remove('show'));
 
+    if (params.get('new') === '1') {
+      if (!API.token()) {
+        location.href = 'login.html?next=' + encodeURIComponent(location.pathname + location.search);
+      } else {
+        openModal();
+      }
+    }
+
     function openModal() {
       // post types
       const pt = document.getElementById('fPostType');
diff --git a/frontend/inbox.html b/frontend/inbox.html
index 5046ff2..61dfe24 100644
--- a/frontend/inbox.html
+++ b/frontend/inbox.html
@@ -147,6 +147,7 @@
     let currentConvo = null;
     let messages = [];
     let pollTimer = null;
+    let lastFetchAt = null; // polling cursor — tracked independently of messages.length
     let paymentConfig = { bkashNumber: '', nagadNumber: '', instructions: '' };
 
     async function init() {
@@ -221,6 +222,7 @@
         return openConvo(id);
       }
       currentConvo = c;
+      lastFetchAt = null;
       history.replaceState(null, '', 'inbox.html?c=' + id);
 
       // Mobile slide view
@@ -263,18 +265,24 @@
     async function loadMessages(full) {
       if (!currentConvo) return;
       const path = '/api/messages/' + currentConvo._id +
-        (!full && messages.length ? '?since=' + encodeURIComponent(messages[messages.length - 1].createdAt) : '');
+        (!full && lastFetchAt ? '?since=' + encodeURIComponent(lastFetchAt) : '');
       try {
         const { messages: fresh } = await API.get(path);
         if (full) {
           messages = fresh || [];
+          if (messages.length) lastFetchAt = messages[messages.length - 1].createdAt;
           renderMessages();
           scrollToBottom();
         } else if (fresh && fresh.length) {
           const hadScrollAtBottom = isScrollAtBottom();
-          messages = messages.concat(fresh);
-          renderMessages();
-          if (hadScrollAtBottom) scrollToBottom();
+          const knownIds = new Set(messages.map(m => m._id));
+          const deduped = fresh.filter(m => !knownIds.has(m._id));
+          if (deduped.length) {
+            messages = messages.concat(deduped);
+            renderMessages();
+            if (hadScrollAtBottom) scrollToBottom();
+          }
+          lastFetchAt = fresh[fresh.length - 1].createdAt;
         }
       } catch { /* polling silence */ }
     }
@@ -384,9 +392,12 @@
       chatInputText.value = '';
       try {
         const { message } = await API.post('/api/messages/' + currentConvo._id, { text });
-        messages.push(message);
-        renderMessages();
-        scrollToBottom();
+        if (!messages.some(m => m._id === message._id)) {
+          messages.push(message);
+          renderMessages();
+          scrollToBottom();
+        }
+        lastFetchAt = message.createdAt;
       } catch (err) {
         toast(err.message, 'error');
       }
@@ -414,9 +425,12 @@
           imageUrl: image.url,
           imagePublicId: image.publicId,
         });
-        messages.push(message);
-        renderMessages();
-        scrollToBottom();
+        if (!messages.some(m => m._id === message._id)) {
+          messages.push(message);
+          renderMessages();
+          scrollToBottom();
+        }
+        lastFetchAt = message.createdAt;
         toast('Photo sent!', 'success');
       } catch (err) {
         toast('Upload failed: ' + err.message, 'error');
diff --git a/frontend/index.html b/frontend/index.html
index 330cfb9..a5e2200 100644
--- a/frontend/index.html
+++ b/frontend/index.html
@@ -114,6 +114,36 @@
     </div>
   </section>
 
+  <!-- ── Unified Live Feed Section ───────────────────────────────────────── -->
+  <section class="section" id="feedSection" style="padding-top:52px;">
+    <div class="wrap">
+      <div class="section-head reveal" style="display:flex;align-items:flex-end;justify-content:space-between;gap:24px;flex-wrap:wrap;text-align:left;">
+        <div>
+          <div class="eyebrow" style="color:var(--amber);background:var(--amber-dim);border-color:rgba(255,176,32,.3);">
+            <span class="dot" style="background:var(--amber);"></span> CAMPUS FEED
+          </div>
+          <h2>Latest offers &amp; requests</h2>
+          <p>Filter by segment, search listings, or view all recent student posts in real time.</p>
+        </div>
+        <button class="btn btn-primary" id="homeNewPostBtn" style="flex-shrink:0;">+ New Post</button>
+      </div>
+
+      <!-- Segment pill strip -->
+      <div class="segment-pills" id="segmentPills" role="tablist" aria-label="Filter by segment"></div>
+
+      <!-- Unified feed grid -->
+      <div class="home-feed-section">
+        <div id="homeFeed" class="post-grid"></div>
+        <p id="homeFeedEmpty" class="muted center hidden" style="padding:48px 0;">
+          No posts found. Be the first to post!
+        </p>
+        <p id="homeFeedLoading" class="muted center" style="padding:48px 0;">
+          Loading recent campus posts…
+        </p>
+      </div>
+    </div>
+  </section>
+
   <!-- ── How It Works ─────────────────────────────────────────────────────── -->
   <section class="section" id="how">
     <div class="wrap">
@@ -292,33 +322,6 @@
     </div>
   </section>
 
-  <!-- ── Unified Live Feed Section ───────────────────────────────────────── -->
-  <section class="section" id="feedSection">
-    <div class="wrap">
-      <div class="section-head reveal">
-        <div class="eyebrow" style="color:var(--amber);background:var(--amber-dim);border-color:rgba(255,176,32,.3);">
-          <span class="dot" style="background:var(--amber);"></span> CAMPUS FEED
-        </div>
-        <h2>Latest offers &amp; requests</h2>
-        <p>Filter by segment, search listings, or view all recent student posts in real time.</p>
-      </div>
-
-      <!-- Segment pill strip -->
-      <div class="segment-pills" id="segmentPills" role="tablist" aria-label="Filter by segment"></div>
-
-      <!-- Unified feed grid -->
-      <div class="home-feed-section">
-        <div id="homeFeed" class="post-grid"></div>
-        <p id="homeFeedEmpty" class="muted center hidden" style="padding:48px 0;">
-          No posts found. Be the first to post!
-        </p>
-        <p id="homeFeedLoading" class="muted center" style="padding:48px 0;">
-          Loading recent campus posts…
-        </p>
-      </div>
-    </div>
-  </section>
-
   <!-- ── Footer ──────────────────────────────────────────────────────────── -->
   <footer>
     <div class="wrap">
@@ -448,6 +451,20 @@
       bgLoop();
     }
 
+    // ── "+ New Post" (home page) ────────────────────────────────────────────
+    const homeNewPostBtn = document.getElementById('homeNewPostBtn');
+    if (homeNewPostBtn) {
+      homeNewPostBtn.addEventListener('click', () => {
+        const targetSegment = activeSegment || (SEGMENTS[0] && SEGMENTS[0].key) || 'tuition';
+        const dest = 'feed.html?segment=' + encodeURIComponent(targetSegment) + '&new=1';
+        if (!API.token()) {
+          location.href = 'login.html?next=' + encodeURIComponent(dest);
+          return;
+        }
+        location.href = dest;
+      });
+    }
+
     // ── Segment Pills & Unified Feed ───────────────────────────────────────
     let activeSegment = '';
 
@@ -538,7 +555,7 @@
           const pt = segCfg.postTypes.find(t => t.key === p.postType);
           return pt ? pt.label : p.postType;
         })();
-        return `<article class="post-card">
+        return `<article class="post-card" data-post-id="${p._id}" tabindex="0" role="link" aria-label="${esc(p.title)}">
           ${thumb}
           <div class="body">
             <h3><a href="post.html?id=${p._id}">${esc(p.title)}</a></h3>
@@ -556,6 +573,23 @@
           </div>
         </article>`;
       }).join('');
+
+      wireCardNavigation(feedEl);
+    }
+
+    // Whole-card click/keyboard navigation, while letting inner links
+    // (author profile) and the photo (lightbox) keep their own behavior.
+    function wireCardNavigation(container) {
+      container.querySelectorAll('.post-card').forEach((card) => {
+        const go = () => { location.href = 'post.html?id=' + card.dataset.postId; };
+        card.addEventListener('click', go);
+        card.addEventListener('keydown', (e) => {
+          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); go(); }
+        });
+        card.querySelectorAll('a, .thumb').forEach((el) => {
+          el.addEventListener('click', (e) => e.stopPropagation());
+        });
+      });
     }
 
     loadHomeFeed();
diff --git a/frontend/js/notifications.js b/frontend/js/notifications.js
index 8edb426..bc297da 100644
--- a/frontend/js/notifications.js
+++ b/frontend/js/notifications.js
@@ -86,12 +86,15 @@
   // ── badge number ───────────────────────────────────────────────────────────
   function updateBadge(count) {
     const badge = document.getElementById('notif-badge');
+    const btn = document.getElementById('notifBtn');
     if (!badge) return;
     if (count > 0) {
       badge.textContent = count > 99 ? '99+' : count;
       badge.classList.remove('hidden');
+      if (btn) btn.classList.add('has-unread');
     } else {
       badge.classList.add('hidden');
+      if (btn) btn.classList.remove('has-unread');
     }
   }
 
diff --git a/frontend/js/ui.js b/frontend/js/ui.js
index a216ad4..dabb834 100644
--- a/frontend/js/ui.js
+++ b/frontend/js/ui.js
@@ -27,6 +27,8 @@
     logout: `<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block;vertical-align:middle;margin-right:4px;"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>`,
     sun: `<svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="5"></circle><line x1="12" y1="1" x2="12" y2="3"></line><line x1="12" y1="21" x2="12" y2="23"></line><line x1="4.22" y1="4.22" x2="5.64" y2="5.64"></line><line x1="18.36" y1="18.36" x2="19.78" y2="19.78"></line><line x1="1" y1="12" x2="3" y2="12"></line><line x1="21" y1="12" x2="23" y2="12"></line><line x1="4.22" y1="19.78" x2="5.64" y2="18.36"></line><line x1="18.36" y1="5.64" x2="19.78" y2="4.22"></line></svg>`,
     moon: `<svg viewBox="0 0 24 24" width="15" height="15" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"></path></svg>`,
+    bell: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 8a6 6 0 0 0-12 0c0 7-3 9-3 9h18s-3-2-3-9"></path><path d="M13.73 21a2 2 0 0 1-3.46 0"></path></svg>`,
+    plus: `<svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" style="display:inline-block;vertical-align:middle;margin-right:4px;"><line x1="12" y1="5" x2="12" y2="19"></line><line x1="5" y1="12" x2="19" y2="12"></line></svg>`,
   };
 
   window.getIcon = function (name) {
@@ -144,7 +146,7 @@
                  }</a>
                  <div class="notif-wrap" id="notif-wrap">
                    <button class="notif-bell-btn" id="notifBtn" aria-label="Notifications" title="Notifications">
-                     🔔
+                     ${ICONS.bell}
                      <span class="notif-badge hidden" id="notif-badge">0</span>
                    </button>
                    <div class="notif-panel" id="notif-panel">
